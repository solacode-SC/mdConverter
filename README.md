# MD Converter

A free, no-auth document converter that transforms PDF, DOCX, and PPTX to Markdown.

## Features
- Convert PDF, Word, and PowerPoint to Markdown
- No authentication or database (fully stateless)
- Clean, fast UI

## Development
Run with make:
```bash
make up
```

Then visit http://localhost:3000
